[project]
name = "empathic-ide"
version = "0.1.0"
description = "An experimental empathic AI-enhanced IDE"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "google-genai>=1.47.0",
    "python-dotenv>=1.2.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
